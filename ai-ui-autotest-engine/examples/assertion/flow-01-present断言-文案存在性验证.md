# Flow01 · present 文案存在性验证

> 执行类型：deterministic

## 落地入口

```
imeituan://www.meituan.com/mrn?mrn_biz=hotel&mrn_entry=hotelchannel-orderfill-duo&mrn_component=main&goods_id=1172344601&checkinDate={T+10:%Y-%m-%d}&checkoutDate={T+11:%Y-%m-%d}&biz_type=1&room_num=1
```

## 执行步骤

### S1. 验证精确匹配文案存在

**操作**：无（已在页面）。

**等待**：页面加载完成。

**校验**：
- 「房型详情」— 文案断言（应通过：精确匹配页面已有文案）

### S2. 验证部分匹配文案存在

**操作**：无（已在页面）。

**等待**：页面渲染完成。

**校验**：
- 「漫曲」— 文案断言（应通过：部分匹配酒店名称）

### S3. 打开弹窗验证深层文案

**操作**：点击「房型详情」。

**等待**：弹窗加载完成。

**校验**：
- 「房型设施详情」— 文案断言（应通过：层级较深元素的匹配）

### S4. 验证不存在的文案

**操作**：无（已在页面）。

**等待**：页面渲染完成。

**校验**：
- 「这是一个绝对不存在的文案测试用例」— 文案断言（预期失败：验证present未命中行为）

### S5. 滚动到底部验证动态文案

**操作**：滚动到底部。

**等待**：动态内容加载完成。

**校验**：
- 「没有更多了」— 文案断言（应通过：动态加载后渲染的文案）
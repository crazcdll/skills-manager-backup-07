# 前后端接口文档

> 文档更新时间：{{TODAY}}
> Owner：{{OWNER}}
> 适用范围：`{{REPO_NAME}}` 核心接口清单
> 关联代码：`src/...`
> 关联文档：`knowledge/business/main-page.md`

## 接口配置入口

说明接口请求入口、通用配置和鉴权方式。

## 核心接口列表

建议按业务区域分节编写，每条至少补齐：

- 接口路径
- 请求方式
- 触发时机
- 关键参数
- 关键响应字段
- 关联模块

## 请求通用配置

说明 headers、traceId、公共参数、预取或缓存机制。

## 失败兜底

说明接口失败后的页面表现和回退逻辑。

# {{MODULE_NAME}} 模块

> 文档更新时间：{{TODAY}}
> Owner：{{OWNER}}
> 适用范围：`{{REPO_NAME}}` 核心模块 / `{{MODULE_SLUG}}`
> 关联代码：`src/...`
> 关联文档：`knowledge/Agent.md`

## 概述

说明模块承担的业务职责和渲染位置。

## 主容器

| 属性 | 值 |
|------|----|
| 容器路径 | `src/...` |
| 入口 | `src/...` |
| 渲染位置 | 待补充 |

## 组件树结构

用树形结构说明模块主要子组件。

## 接口与数据

如果无独立接口，请明确写“无独立接口，数据来自 xxx 模块 / xxx 接口”。

| 接口路径 | 场景 | 关键字段 |
|----------|------|----------|
| `/api/example` | 示例 | `data.title` |

## 关键字段

列出对渲染、交互、状态切换影响最大的字段；不要少于 3 个关键字段。

## 交互逻辑

- 交互 1
- 交互 2
- 交互 3

## Store 连接

说明依赖哪些 state、action、saga；如果不依赖 Store，请说明局部 state / props 来源。

## 跨端差异

说明不同端是否有差异化逻辑。

## 联动模块

列出强耦合模块或上下游模块。

## 常见问题 / 易错点

- 至少补 1 条真实排障点

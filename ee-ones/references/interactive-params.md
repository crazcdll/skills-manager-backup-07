# 写操作参数清单与交互流程

> **加载时机**：当 Agent 需要执行**创建、更新、删除**等写操作，需要确认必填参数、交互流程或字段取值范围时，读取本文件。

---

## 创建工作项（wc）

**交互流程**：选择空间 → **⛔ 空间权限前置校验** → 选择子类型 → 填写必填字段 → 确认预览 → 提交

> ⚠️ **空间权限前置校验**：`wc` 命令在参数解析后会**立即校验**用户是否有目标空间的权限（`assertSpacePermission`），无权限时直接阻断退出，不会继续执行后续的字段校验、界面方案查询等重操作。这避免了用户填了大量参数后才发现无权限的糟糕体验。

| 参数 | 说明 | 必填 |
|------|------|------|
| `-t, --type` | 类型: REQUIREMENT / DEVTASK / DEFECT（支持中文：需求/任务/缺陷/bug） | 是 |
| `-p, --project-id` | 空间 ID（跳过空间选择） | 推荐 |
| `-s, --space-name` | 空间名称关键字 | 替代 -p |
| `--title` | 标题 | 推荐预填 |
| `--desc` | 描述 | 可选 |
| `--priority` | 优先级：低/中/高/紧急 或 1~4 | 可选 |
| `--severity` | 严重程度（缺陷专属）: BLOCKER/CRITICAL/MAJOR/NORMAL/TRIVIAL | 缺陷时可选 |
| `--assigned` | 指派给（MIS 号） | 可选 |

> **自动化建议**：尽量提供 `-p`、`--title`、`--priority`，减少交互步骤。

### ⚠️ 字段取值规则（创建 & 更新通用）

以下规则适用于 `wc`（创建）和 `wu`（更新）中通过 `-F / --field` 传入的自定义字段：

**1. 普通下拉字段（list / component_* 等）**

所有下拉接口返回的结构都是 `{ value, displayValue }`，**实际填值必须使用 `value`**，不能用 `displayValue`。

```bash
# 先查可选值
ones fo -p <空间ID> -f <字段ID> -t <字段类型>
# 返回示例: [{ "value": 42, "displayValue": "高优方案" }, ...]
# 传入 fieldValue 时使用 value（42），不是 displayValue（"高优方案"）
```

**2. 级联字段（cascader 类型）**

级联字段有多层级可选值（children 嵌套），**只允许选择最末级节点**（无 children 或 children 为空的节点）。

传值规则：**各层级的 value 用 `^` 拼接**，从第一层到最末层。

```bash
# 假设级联结构：
#   产品线A (value: "1")
#     └─ 子模块X (value: "2")
#           └─ 功能点Y (value: "3")   ← 最末级
#
# 传值：fieldValue = "1^2^3"
-F '{"id":1234,"name":"所属产品","variable":"customField1234","type":"cascader","multiple":false,"fieldValue":"1^2^3"}'
```

> ⚠️ **严禁选择非末级节点**。`^` 是唯一合法的层级分隔符，不能用 `/` 或其他字符。

**3. 需求提出部门字段（component_new_org 类型，如 customField23388）**

此类型字段的值是**组织 ID（数字）**，不是组织名称。获取组织 ID 的方式：

- 用户提供**完整部门链**（如 `美团/核心本地商业/基础研发平台/研发质量与效率部`）
  > 💡 完整部门链可从**大象个人名片**中查找（如：美团/行政/行政BP中心）
- 通过接口查询：`GET /api/proxy/org/getOrgByPath?path=<URL编码路径>`
- 取返回值中的 `id` 作为 `fieldValue`

```bash
# 示例：用户提供路径 "美团/核心本地商业/基础研发平台/研发质量与效率部"
# 接口返回 { "id": 40000923, "name": "研发质量与效率部", ... }
# 传值：fieldValue = 40000923
-F '{"id":23388,"name":"需求提出部门","variable":"customField23388","type":"component_new_org","multiple":false,"fieldValue":40000923}'
```

> Agent 在遇到 `component_new_org` 或 `component_org` 类型必填字段时，应提示用户提供完整部门链（可从**大象个人名片**中查找，如：美团/行政/行政BP中心），然后调用上述接口获取 ID。

---

## 更新工作项（wu）

> ⚠️ **空间权限前置校验**：`wu` 命令在解析工作项 ID 后，会**立即查询工作项详情**获取所属空间并校验权限（`getIssueDetail` 内含 `assertSpacePermission`），无权限时直接阻断退出，不会进入级联字段校验、预计工作量换算等重操作。

| 参数 | 说明 | 必填 |
|------|------|------|
| `-i, --id` | 工作项 ID | 是 |
| `--name` | 工作项标题 | 可选 |
| `--desc` | 描述信息（支持 HTML） | 可选 |
| `--priority` | 优先级: 低(1) / 中(2) / 高(3) / 紧急(4) | 可选 |
| `--assigned` | 指派给（用户 MIS 号） | 可选 |
| `--expect-time` | 预计工作量（8h / 1d / 0.5d / 纯数字），自动根据空间工时配置换算 | 可选 |
| `-F, --field` | 自定义字段（WorkitemValueField JSON，可多次使用） | 可选 |
| `-y, --yes` | 跳过确认直接更新 | 自动化时推荐 |

**注意**：
- 至少提供一个更新字段（`--name` / `--desc` / `--priority` / `--assigned` / `--expect-time` / `-F`）
- 预计工作量会自动获取工作项详情确定空间 ID，再根据空间工时配置换算单位
- `-F` 参数接收 JSON 格式：`{"variable":"...","name":"...","type":"...","multiple":false,"fieldValue":...}`
- 查询字段元信息：`ones fs -p <空间ID> -t <工作项类型>`；查询可选值：`ones fo -p <空间ID> -f <字段ID>`

---

## 删除工作项（wd）

| 参数 | 说明 |
|------|------|
| `-i, --id` | 工作项 ID |
| `-c, --delete-children` | 同时删除子工作项 |
| `-y, --yes` | 跳过确认直接删除 |

> ⚠️ 删除不可恢复，agent 执行前应先 `workitem-detail` 确认。

---

## 创建迭代（ci）

**交互流程**：输入空间ID → 名称 → 日期范围 → 描述 → 人力配置 → 确认

| 参数 | 说明 |
|------|------|
| `-p` | 空间 ID |
| `-n` | 迭代名称 |
| `-s` | 开始日期（YYYY-MM-DD） |
| `-e` | 结束日期（YYYY-MM-DD） |
| `-d` | 描述 |
| `--hr` | 人力配置: last（默认）/ project |
| `-y` | 跳过确认 |

> 自动化时传入全参数 + `-y`。

---

## 编辑迭代（ui）

| 参数 | 说明 |
|------|------|
| `-p` | 空间 ID |
| `-i` | 迭代 ID（通过 `iter --json` 获取） |
| `-n` | 新名称 |
| `-s` / `-e` | 新日期 |
| `-d` | 新描述 |
| `-y` | 跳过确认 |

---

## 设置工作项迭代（si）

| 参数 | 说明 |
|------|------|
| `-i` | 工作项 ID |
| `-t` | 目标迭代 ID |
| `--clear` | 清空迭代（移出） |
| `--sync-child` | 同步修改子工作项 |
| `-y` | 跳过确认 |

---

## 创建用例（case-c）

**交互流程**：选择空间 → 选择子类型 → 填写字段（标题/优先级/描述/自定义字段）→ 确认预览 → 提交

| 参数 | 说明 | 必填 |
|------|------|------|
| `-p, --project-id` | 空间 ID | 非交互模式必填 |
| `-s, --space-name` | 空间名称关键字 | 替代 -p |
| `--title` | 标题 | 非交互模式下必填字段必须通过参数提供 |
| `--desc` | 描述 | 可选 |
| `--priority` | 优先级: 低/中/高/紧急 或 1~4 | 可选 |
| `-y, --yes` | 非交互模式：跳过所有交互 | Agent 自动化时推荐 |

> **非交互模式（`-y`）行为**：
> - **空间**：必须通过 `-p` 指定，不会触发空间选择交互
> - **子类型**：自动选择默认子类型（或首个），不会触发选择交互
> - **字段填写**：使用命令行参数和 API 默认值，必填字段未提供则报错退出
> - **列表字段**：值必须在 API 返回的可选值范围内，否则报错并列出合法值
> - **用户字段**：通过预填关键字自动搜索用户，精确匹配用户名
> - **确认预览**：跳过确认步骤，自动提交

---

## 更新用例（case-u）

**交互流程**：查询用例详情 → 选择要修改的字段 → 输入新值 / 从可选值中选择 → 确认更新

| 参数 | 说明 | 必填 |
|------|------|------|
| `-i, --id` | 用例 ID | 是 |
| `--space` | 空间 ID（可选，自动从详情中提取） | 可选 |
| `-f, --field` | 要修改的字段名（标题 / 优先级 / 状态） | 非交互模式必填 |
| `--val` | 新值（直接指定，跳过交互式选择） | 非交互模式必填 |
| `-y, --yes` | 非交互模式 | Agent 自动化时推荐 |

> **非交互模式（`-y`）行为**：
> - 必须同时提供 `-f` 和 `--val`
> - 优先级、状态等列表字段的值必须在 API 返回的可选值范围内，支持模糊匹配（如 "高" 可匹配 "🟠 高"）
> - 值不合法则报错并列出合法值

---

## 创建测试计划（plan-c）

| 参数 | 说明 | 必填 |
|------|------|------|
| `-c, --scene` | 场景: REQUIREMENT（需求类）/ SCENE（场景类） | 非交互模式必填 |
| `-p` | 空间 ID | 非交互模式必填 |
| `--title` | 标题 | 非交互模式必填 |
| `--principal` | 测试主R（默认当前用户） | 可选 |
| `--requirement-ids` | 关联需求 ID（逗号分隔，仅需求类） | 可选 |
| `--desc` | 描述 | 可选 |
| `--start-time` / `--end-time` | 预计时间（YYYY-MM-DD） | 可选 |
| `-y, --yes` | 非交互模式：跳过确认 | Agent 自动化时推荐 |

> **非交互模式（`-y`）行为**：
> - **场景**：必须通过 `--scene` 指定（REQUIREMENT 或 SCENE），值不合法则报错退出
> - **空间**：必须通过 `-p` 指定
> - **标题**：必须通过 `--title` 指定
> - **测试主R**：未传 `--principal` 则默认使用当前用户
> - **描述/日期**：未传则跳过，日期格式无效会报错退出
> - **确认预览**：跳过确认步骤，自动提交

---

## 更新测试计划（plan-u）

**交互流程**：查询计划详情 → 选择要修改的字段 → 输入新值 → 确认更新

| 参数 | 说明 | 必填 |
|------|------|------|
| `-i, --id` | 计划 ID | 是 |
| `-p, --project-id` | 空间 ID | 是 |
| `-f, --field` | 要修改的字段名（标题/测试主R/描述/预计开始时间/预计结束时间） | 非交互模式必填 |
| `--val` | 新值（直接指定） | 非交互模式必填 |
| `-y, --yes` | 非交互模式 | Agent 自动化时推荐 |

> **非交互模式（`-y`）行为**：
> - 必须同时提供 `-i`、`-p`、`-f`、`--val`
> - 可更新字段：标题、测试主R、描述、预计开始时间、预计结束时间
> - 日期格式无效会报错退出

---

## 创建测试轮次（round-c）

| 参数 | 说明 |
|------|------|
| `-p` | 空间 ID |
| `--plan-id` | 所属计划 ID |
| `-t, --type` | 类型: SMOKE_TEST / FUNCTION_TEST / REGRESSION_TEST |
| `--title` | 名称 |

---

## 更新轮次（round-u）

| 参数 | 说明 |
|------|------|
| `-i` | 轮次 ID |
| `-p` | 空间 ID |
| `--name` | 新名称 |

---

## 删除轮次（round-d）

| 参数 | 说明 |
|------|------|
| `-i` | 轮次 ID |
| `-p` | 空间 ID |
| `-y` | 跳过确认 |

> ⚠️ 删除轮次不可恢复。

---

## 轮次用例操作

### 添加/删除用例（round-ca）

| 参数 | 说明 |
|------|------|
| `-p` | 空间 ID |
| `-r` | 轮次 ID |
| `--plan-id` | 计划 ID |
| `-a` | 操作: ADD / DEL |
| `--case-ids` | 用例 ID 列表（逗号分隔，仅 ADD 操作，配合 -y 使用） |
| `--items` | 执行用例ID:用例ID 对（逗号分隔，仅 DEL 操作，配合 -y，如 "100:200,101:201"） |
| `-y` | 非交互模式：跳过确认，必须提供 -a 和 --case-ids/--items |

### 更新执行状态（round-cu）

| 参数 | 说明 |
|------|------|
| `-p` | 空间 ID |
| `-r` | 轮次 ID |
| `-s` | 目标状态: UNDONE / SUCCESS / FAIL / LATER |
| `--items` | 执行用例ID:用例ID 对（逗号分隔，配合 -y，如 "100:200,101:201"） |
| `-y` | 非交互模式：跳过确认，必须提供 -s 和 --items |

### 更新执行人（round-ce）

| 参数 | 说明 |
|------|------|
| `-r` | 轮次 ID |
| `--items` | 用例ID:执行人 对（逗号分隔，配合 -y，如 "100:zhangsan,101:lisi"） |
| `-y` | 非交互模式：跳过确认，必须提供 -r 和 --items |

> 交互模式下交互式选择执行人。

### 关联缺陷（round-cli）

| 参数 | 说明 |
|------|------|
| `-p` | 空间 ID |
| `--plan-id` | 计划 ID |
| `-r` | 轮次 ID |
| `-c` | 执行用例 ID |
| `-i` | 缺陷 ID |

---

## 创建提测单（stc）

**交互流程**：选择空间 → 选择子类型 → 关联工作项（必填，支持多选，可关联分支）→ 填写必填字段 → 确认

| 参数 | 说明 |
|------|------|
| `-p` | 空间 ID |
| `-s` | 空间名称关键字 |
| `--title` | 标题 |
| `-y` | 跳过确认 |

> 创建提测单时必须关联至少一个工作项。

---

## 更新提测状态（stu）

**交互流程**：查询详情 → 获取可流转状态列表 → 选择目标状态 → 检查必填字段 → 执行

| 参数 | 说明 |
|------|------|
| `-p` | 空间 ID（必填） |
| `-i` | 提测单 ID（必填） |
| `-s` | 目标状态名称（模糊匹配） |
| `-y` | 跳过确认 |

> 若当前提测单已是终态或无权限，则无可流转选项。

---

## 分支创建与关联

### 创建分支（bc）

**流程**：**获取当前仓库 remote 地址** → 查询工作项详情 → 搜索空间应用获取 appId → 生成分支名 → 创建并关联

| 参数 | 说明 |
|------|------|
| `-i` | 工作项 ID（必填） |
| `-t` | 分支类型（默认 feature） |
| `-a` | 应用 ID（不传则交互选择，通过 `apps` 命令获取） |
| `-b` | 基础分支（默认 master） |

> **⭐ Agent 获取应用 ID 策略**：
> 1. **优先获取当前仓库 remote 地址**：执行 `git remote get-url origin`，将返回的完整地址直接作为 `-n` 参数传入，无需提取仓库名（如 `ones apps -p <空间ID> -n "ssh://git@git.sankuai.com/ee/ones-cli.git"`）
> 2. 从搜索结果中取 `id` 字段作为应用 ID
> 3. **降级**：若不在 git 仓库中或无 remote，再询问用户提供仓库名或 appkey
> 4. **搜索无结果时禁止自行更换关键词重试**，应立即告知用户搜索失败并请提供准确的仓库名或 appkey

### 关联已有分支（ba）

| 参数 | 说明 |
|------|------|
| `-n` | 分支名称（必填） |
| `-p` | 空间 ID（必填） |
| `-a` | 应用 ID（必填，**优先用当前仓库 remote 地址搜索应用**） |
| `-t` | 工作项 ID（必填） |
| `--branch-type` | 分支类型（默认 feature） |

> 关联前确保分支已推送到远程仓库。应用 ID 获取策略与创建分支相同：**优先用当前仓库 remote 地址搜索**。

---

## 工时填写（wtadd）

| 参数 | 说明 | 必填 |
|------|------|------|
| `-i` | 工作项 ID | 是 |
| `--hours` | 工时数：8h / 1d / 纯数字 | 是 |
| `-d` | 日期，默认今天；范围: YYYY-MM-DD~YYYY-MM-DD | 可选 |
| `--add-type` | 填写类型，默认 TOTAL | 可选 |
| `--desc` | 工时描述 | 可选 |
| `--include-weekend` | 包含周末（默认 false） | 可选 |
| `-y` | 跳过确认（周末默认不包含） | 自动化时推荐 |

> 提交前会自动执行参数校验（preValidations），校验不通过会阻断提交。

---

## 替他人填写工时（wtadd-for）

**交互流程**：输入被填人MIS → 输入工作项ID → 自动获取工作项类型和空间 → 获取工时配置 → 输入工时数 → 日期确认 → 周末确认（如适用）→ 参数校验 → 提交

| 参数 | 说明 | 必填 |
|------|------|------|
| `--operator` | 被填工时人的 MIS 号 | 是 |
| `-i` | 工作项 ID | 是 |
| `--hours` | 工时数：8h / 1d / 纯数字 | 是 |
| `-d` | 日期，默认今天；范围: YYYY-MM-DD~YYYY-MM-DD | 可选 |
| `-t, --issue-type` | 工作项类型（自动获取，一般无需指定） | 可选 |
| `-p, --space-id` | 空间 ID（自动获取，一般无需指定） | 可选 |
| `--add-type` | 填写类型，默认 TOTAL | 可选 |
| `--desc` | 工时描述 | 可选 |
| `--include-weekend` | 包含周末（默认 false） | 可选 |
| `-y` | 跳过交互确认（周末默认不包含） | 自动化时推荐 |

> ⚠️ 提交前同样执行参数校验。

---

## 工时修改（wtu）

| 参数 | 说明 | 必填 |
|------|------|------|
| `--id` | 工时记录 ID（通过 `wtd` 获取） | 是 |
| `-i` | 工作项 ID | 是 |
| `--hours` | 新的工时数 | 可选 |
| `-d` | 日期 | 可选 |
| `--desc` | 描述 | 可选 |

---

## 工时删除（wtdel）

| 参数 | 说明 |
|------|------|
| `--id` | 工时记录 ID（通过 `wtd` 获取） |
| `-y` | 跳过确认 |

> ⚠️ 工时记录 ID 必须通过 `ones wtd -i <工作项ID>` 查询获取。

---

## 新建排期事项（sic）

**交互流程**：输入空间ID/工作项ID → 自动确保排期存在 → 指定阶段 → 创建事项 → 展示结果

| 参数 | 说明 | 必填 |
|------|------|------|
| `-p, --space-id` | 空间 ID | 是 |
| `-i, --issue-id` | 工作项 ID | 是 |
| `-s, --stage` | 排期阶段: DESIGNING(规划) / DEVELOPING(开发) / TESTING(测试) / PUBLISHING(发布) | 是 |
| `--index` | 排序索引（默认 0） | 可选 |

> 创建后可通过 `siu` 命令设置事项的名称、工作量和预计时间。

---

## 更新排期事项（siu）

| 参数 | 说明 | 必填 |
|------|------|------|
| `-p, --space-id` | 空间 ID | 是 |
| `-i, --issue-id` | 工作项 ID | 是 |
| `--item-id` | 排期事项 ID（通过 `sq --json` 获取） | 是 |
| `--name` | 事项名称 | 可选 |
| `--assigned` | 指派给（MIS 号，仅父事项可改） | 可选 |
| `--expect-time` | 预计工作量（单位小时，建议设置在子事项上） | 可选 |
| `--expect-start` | 预计开始时间（YYYY-MM-DD，建议设置在子事项上） | 可选 |
| `--expect-close` | 预计结束时间（YYYY-MM-DD，建议设置在子事项上） | 可选 |

> **注意**：
> - 排期事项分为父事项（阶段容器）和子事项（工序），预计工作量和时间应在子事项上设置
> - 子事项（工序）的指派人不允许修改，接口会报错"排期事项下的排期工序指派人不允许修改"
> - 至少指定一个修改字段，否则命令会报错

---

## 删除排期事项（sid）

| 参数 | 说明 | 必填 |
|------|------|------|
| `-p, --space-id` | 空间 ID | 是 |
| `-i, --issue-id` | 工作项 ID | 是 |
| `--item-id` | 排期事项 ID（通过 `sq --json` 获取） | 是 |
| `-y, --yes` | 跳过二次确认直接删除 | 可选 |

> ⚠️ 删除排期事项不可恢复。

---

## 修改排期字段（sctu）

| 参数 | 说明 | 必填 |
|------|------|------|
| `-p, --space-id` | 空间 ID | 是 |
| `-i, --issue-id` | 工作项 ID | 是 |
| `--design-time` | UI 交付时间（YYYY-MM-DD） | 可选 |
| `--test-time` | 提测时间（YYYY-MM-DD） | 可选 |
| `--release-time` | 上线时间（YYYY-MM-DD） | 可选 |
| `--develop-start-time` | 开发开始时间（YYYY-MM-DD） | 可选 |
| `--complete-time` | 完成时间（YYYY-MM-DD） | 可选 |
| `--rd-masters` | 技术主R（MIS 号，多人用逗号分隔） | 可选 |
| `--qa-masters` | 测试主R（MIS 号，多人用逗号分隔） | 可选 |
| `--implement-type` | 技术实现方式（传可选值名称或 ID） | 可选 |
| `--rd-self-test` | 研发自测（是/否） | 可选 |

> 至少指定一个修改参数。日期会自动转为当天 23:59:59 的毫秒时间戳。字段由排期模板配置决定。

---

## 上传附件（watt upload）

| 参数 | 说明 | 必填 |
|------|------|------|
| `-i, --issue-id` | 工作项 ID | 是 |
| `-f, --file` | 本地文件路径（绝对/相对路径） | 是 |
| `--json` | 以 JSON 格式输出上传结果 | 可选 |

> 上传前会展示文件名、大小、路径等信息，上传成功后展示附件列表表格。

---

## 删除附件（watt delete）

| 参数 | 说明 | 必填 |
|------|------|------|
| `-i, --issue-id` | 工作项 ID（用于查询附件列表展示确认） | 是 |
| `-a, --attachment-id` | 附件 ID（通过 workitem-detail --json 获取） | 是 |

> ⚠️ 删除附件不可恢复。**强制二次确认**：必须输入 `delete` 确认，无法通过 `-y` 跳过，不适合自动化流程。
> 附件 ID 必须通过 `ones workitem-detail -i <ID> --json` 获取。

---

## 创建大象群（wxg create）

**交互流程**：检查已关联 → 查询字段 → 查询成员 → 展示预览 → 调用接口创建

| 参数 | 说明 | 必填 |
|------|------|------|
| `-i, --issue-id` | 工作项 ID | 是 |
| `-t, --issue-type` | 工作项类型: REQUIREMENT / DEVTASK / DEFECT / PRODUCT_DOCUMENT | 是 |
| `--users` | 手动指定成员 MIS（逗号分隔），不传则自动查询 | 可选 |
| `--admins` | 群管理员 MIS（逗号分隔，默认取成员列表中首个） | 可选 |
| `--owner` | 群主 MIS（默认取成员列表中首个） | 可选 |
| `--user-fields` | 群名/备注中展示的字段名称（逗号分隔），默认取默认勾选字段名 | 可选 |
| `--include-children` | 是否包含子工作项成员（默认 false） | 可选 |
| `--json` | 以 JSON 格式输出创建结果 | 可选 |

> **注意**：
> - 该命令含**幂等检查**：若工作项已关联大象群，则直接展示已关联群聊信息并退出，不会重复创建
> - 不传 `--users` 时，自动按以下顺序获取成员：① 查询可选字段定义（roles 接口）→ ② 筛选 `isSelected=true` 的字段 → ③ 查询这些字段对应的人员 MIS（members 接口）
> - 若自动查询的成员为空，命令会报错并提示使用 `--users` 手动指定
> - 群主 / 管理员不在成员列表时会自动追加，不会报错


---

## 发布计划管理（releaseplan）

发布计划（上线计划）用于管理一批工作项的统一发布。所有写操作均需 `--space <空间ID>` 和 `--plan <计划ID>`，计划 ID 通过 `ones releaseplan list --space <空间ID>` 获取。

### 创建发布计划（rp-c）

| 参数 | 说明 | 必填 |
|------|------|------|
| `--space <spaceId>` | 空间 ID | 是 |
| `-n, --name <name>` | 发布计划名称 | 是 |
| `--start <date>` | 开始时间（YYYY-MM-DD） | 可选 |
| `--end <date>` | 结束时间（YYYY-MM-DD） | 可选 |
| `-y, --yes` | 跳过二次确认 | Agent 必传 |
| `--json` | JSON 格式输出 | 可选 |

### 删除发布计划（rp-del）

| 参数 | 说明 | 必填 |
|------|------|------|
| `--space <spaceId>` | 空间 ID | 是 |
| `--plan <planId>` | 发布计划 ID | 是 |
| `-y, --yes` | 跳过二次确认 | Agent 必传 |

### 锁定 / 解锁发布计划（rp-l / rp-ul）

| 参数 | 说明 | 必填 |
|------|------|------|
| `--space <spaceId>` | 空间 ID | 是 |
| `--plan <planId>` | 发布计划 ID | 是 |
| `-y, --yes` | 跳过二次确认 | Agent 必传 |

> 锁定后无法增删工作项，需先 `unlock` 解锁。

### 添加工作项到发布计划（rp-ai）

| 参数 | 说明 | 必填 |
|------|------|------|
| `--space <spaceId>` | 空间 ID | 是 |
| `--plan <planId>` | 发布计划 ID | 是 |
| `--issues <issueIds>` | 工作项 ID（逗号分隔多个） | 是 |
| `-y, --yes` | 跳过二次确认 | Agent 必传 |
| `--json` | JSON 格式输出 | 可选 |

> **前置条件**：工作项必须**已关联分支**才能添加。建议先执行 `ones releaseplan addable-issues --space <空间ID> --json` 查询可添加的工作项，从中选取 ID。

### 从发布计划移除工作项（rp-ri）

| 参数 | 说明 | 必填 |
|------|------|------|
| `--space <spaceId>` | 空间 ID | 是 |
| `--plan <planId>` | 发布计划 ID | 是 |
| `--issues <issueIds>` | 工作项 ID（逗号分隔多个） | 是 |
| `-y, --yes` | 跳过二次确认 | Agent 必传 |

> **通用说明**：
> - 所有操作均前置校验空间权限，无权限直接阻断
> - 计划 ID 必须先通过 `ones releaseplan list --space <空间ID>` 查询获取
> - 查询类命令（`list`/`detail`/`issues`/`apps`/`addable-issues`）无需 `-y`，支持 `--json`


---
name: 清单依赖检查
description: 检查 @gfe/babel-plugin-react-add-name 是否仍出现在 dependencies.json、ohDependencies.json；通过后再结项
---

## 目标

在**最终汇报**前检查 **`@gfe/babel-plugin-react-add-name`** 是否仍出现在：

- `dependencies.json` 的条目 `name`
- `ohDependencies.json` 的条目 `name`

有 `🚨` 时，结项报告**必须**带上本步结论。

## 检查方式

```bash
grep -E "babel-plugin-react-add-name" dependencies.json ohDependencies.json 2>/dev/null
```

若 `grep` 命中清单文件，需确认命中的是 **`name`** 还是仅 `url` 子串。

## 结论

- `🚨` 若任一清单存在：列出位置，要求从 `dependencies.json`、`ohDependencies.json` 中**彻底删除**。
- `✅` 若两份清单均未发现：输出通过结论。

结论文案追加到 `${{outputFile:.temp/trade-duo-standard-material-upgrade-npm-dependencies/result/changelist.md}}`（可写时）。

*下一步：第 5 步 `05-提示.md`*

---
description: "视觉设计综合层模板"
---
**输入源**：[图片链接/视觉稿链接]

---

## 1 视觉稿图片

![视觉稿图片](图片链接或路径)

---

## 2 整体框架

<!--
  第一步：输出整体骨架 + 整体容器数据。
  子模块内部如有嵌套，继续用缩进方框体现层级，按实际情况拆到最深层组件即可。
-->

**描述**

[一句话描述整体布局方向和主要分区，如「整体采用纵向 flex 布局，从上到下分为顶部信息区、中部内容列表区、底部操作区」]

**骨架**

```
┌──────────────────────────────────────────────────────┐
│  [页面/组件名称]                                      │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │  A: [子模块名]                                   │ │
│  │  ┌─────────────────┐  ┌─────────────────┐        │ │
│  │  │  A-1: [名称]    │  │  A-2: [名称]    │        │ │
│  │  └─────────────────┘  └─────────────────┘        │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │  B: [子模块名]                                   │ │
│  │  ┌──────────────────────────────────────────┐    │ │
│  │  │  B-1: [名称]                             │    │ │
│  │  │  ┌──────────────┐  ┌──────────────┐      │    │ │
│  │  │  │ B-1-a:[名称] │  │ B-1-b:[名称] │      │    │ │
│  │  │  └──────────────┘  └──────────────┘      │    │ │
│  │  └──────────────────────────────────────────┘    │ │
│  │  ┌──────────────────────────────────────────┐    │ │
│  │  │  B-2: [名称]                             │    │ │
│  │  └──────────────────────────────────────────┘    │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │  C: [子模块名]                                   │ │
│  └──────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
```

**间距**

<!-- ⚠️ gap 在 Max 技术栈中禁止使用，子元素间距统一用 margin-bottom（纵向布局）或 margin-right（横向布局）替代 -->
| 位置 | 属性 | 值 |
|------|------|----|
| 容器内边距 | padding | [上] [右] [下] [左] |
| 子模块间距（纵向） | margin-bottom | [N]px |
| 子模块间距（横向） | margin-right | [N]px |
| 外边距 | margin | [N]px |

**排版**

| 元素 | 字号 | 字重 | 颜色 | 行高 | 对齐 |
|------|------|------|------|------|------|
| [标题] | [N]px | [400/500/600/700] | [#色值] | [N]px | [left/center/right] |
| [正文] | [N]px | [400] | [#色值] | [N]px | [left/center/right] |
| [辅助文字] | [N]px | [400] | [#色值] | [N]px | [left/center/right] |

**颜色**

| 用途 | 色值 | 说明 |
|------|------|------|
| 主背景 | [#色值] | 容器/页面背景色 |
| 卡片背景 | [#色值] | 子模块卡片背景色 |
| 强调色 | [#色值] | 按钮/价格/标签等强调元素 |
| 主文字 | [#色值] | 标题/正文颜色 |
| 辅助文字 | [#色值] | 描述/辅助信息颜色 |
| 分割线 | [#色值] | 分隔线/边框颜色 |
| 禁用 | [#色值] | disabled 状态颜色 |

**CSS**

```css
/* [页面/组件名称] */
width: [N]px;                /* 宽度 */
height: [N]px;               /* 高度 */
min-width: [N]px;           /* 最小宽度，无约束则删除此行 */
max-width: [N]px;           /* 最大宽度，无约束则删除此行 */
min-height: [N]px;          /* 最小高度，无约束则删除此行 */
display: flex;               /* 弹性布局 */
flex-direction: [row/column];  /* 主轴方向 */
flex-wrap: [nowrap/wrap];   /* 是否换行 */
align-items: [center/flex-start/flex-end/stretch];  /* 交叉轴对齐 */
justify-content: [flex-start/space-between/space-around/space-evenly/center];  /* 主轴对齐 */
padding: [上] [右] [下] [左];  /* 内边距 */
margin: [上] [右] [下] [左];   /* 外边距 */
border-radius: [N]px;        /* 圆角 */
border: [N]px [solid/dashed] [#色值];  /* 无边框则删除此行 */
box-shadow: [N]px [N]px [N]px [N]px [rgba/color];  /* 无阴影则删除此行 */
background: [颜色值];        /* 背景色 */
overflow: [hidden/visible/scroll];  /* 溢出处理 */
position: [relative/absolute/fixed/sticky];  /* 默认 static 则删除此行 */
z-index: [N];                /* 需要 z-index 时才填写 */
```

**Mock 数据**

```json
{
  "[字段名]": "[示例文字，如'商品标题']",
  "[字段名]": "[图片地址，如'https://example.com/img.png']",
  "[字段名]": [数值，如 99],
  "[字段名]": true
}
```

---

## 3 子模块详细数据

<!--
  第二步：逐个子模块输出详细数据。
  顺序与"整体框架"中的 A / B / C 对应，每个模块包含：
  描述 + 框架 + 间距 + 排版 + CSS + Props + Mock 数据 + 交互状态 + 边界情况。
  若子模块存在嵌套（如 B-1 内还有 B-1-a），则在该子模块下继续展开嵌套层的详细数据。
-->

---

### A: [子模块名]

**描述**

[一句话说明该模块的视觉职责和业务含义，如「顶部信息区，展示商品主图与核心价格，左图右文横向布局」]

**框架**

```
┌──────────────────────────────────────────────────┐
│  A: [子模块名]  flex-[row/column]                │
│  ┌─────────────┐  gap:[N]px  ┌─────────────┐    │
│  │  A-1        │ ──────────▶ │  A-2        │    │
│  └─────────────┘             └─────────────┘    │
└──────────────────────────────────────────────────┘
```

**间距**

<!-- ⚠️ gap 在 Max 技术栈中禁止使用，子元素间距统一用 margin-bottom（纵向布局）或 margin-right（横向布局）替代 -->
| 位置 | 属性 | 值 |
|------|------|----|
| 模块内边距 | padding | [上] [右] [下] [左] |
| 子元素间距（纵向） | margin-bottom | [N]px |
| 子元素间距（横向） | margin-right | [N]px |
| 外边距 | margin | [N]px |

**排版**

| 元素 | 字号 | 字重 | 颜色 | 行高 | 对齐 |
|------|------|------|------|------|------|
| [元素名] | [N]px | [400/500/600/700] | [#色值] | [N]px | [left/center/right] |
| [元素名] | [N]px | [400] | [#色值] | [N]px | [left/center/right] |

**CSS**

```css
/* A: [子模块名] */
width: [N]px;                /* 宽度 */
height: [N]px;               /* 高度 */
min-width: [N]px;           /* 最小宽度，无约束则删除此行 */
max-width: [N]px;           /* 最大宽度，无约束则删除此行 */
min-height: [N]px;          /* 最小高度，无约束则删除此行 */
display: flex;               /* 弹性布局 */
flex-direction: [row/column];  /* 主轴方向 */
flex: [0 0 auto / 1 / N];   /* 弹性伸缩比例，默认 0 0 auto 则删除此行 */
flex-shrink: [0/1];         /* 是否允许收缩，默认 1 */
align-items: [center/flex-start/flex-end/stretch];  /* 交叉轴对齐 */
justify-content: [flex-start/space-between/space-around/space-evenly/center];  /* 主轴对齐 */
padding: [上] [右] [下] [左];  /* 内边距 */
margin: [上] [右] [下] [左];   /* 外边距 */
border-radius: [N]px;        /* 圆角 */
border: [N]px [solid/dashed] [#色值];  /* 无边框则删除此行 */
box-shadow: [N]px [N]px [N]px [N]px [rgba/color];  /* 无阴影则删除此行 */
background: [颜色值];        /* 背景色 */
overflow: [hidden/visible/scroll];  /* 溢出处理 */
position: [relative/absolute/sticky];  /* 默认 static 则删除此行 */
```

**Props**

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `[propName]` | `[string/number/boolean]` | `[默认值]` | [说明] |

**推荐组件**：

| 组件名称 | 类型 | 组件属性与值 | 版本 |
|--------|------|--------|------|
| Max/Leez 组件名 |  Max/Leez | <属性:值> | 组件版本 |

**Mock 数据**

```json
{
  "[字段名]": "[示例文字，如'商品标题']",
  "[字段名]": "[图片地址，如'https://example.com/img.png']",
  "[字段名]": [数值，如 99],
  "[字段名]": true
}
```

**交互状态**

| 状态 | 视觉变化 | 过渡 |
|------|----------|------|
| 默认 | [描述默认样式] | [如「无」，无则填「—」] |
| 按压 / active | [如「背景色加深为 #色值，透明度 0.8」] | [如「200ms ease」] |
| 禁用 / disabled | [如「整体透明度 0.4，不可点击」] | [如「200ms ease」] |
| 加载 / loading | [如「文字替换为菊花图，不可点击」，无则填「—」] | [如「300ms ease-in-out」] |

**边界情况**

- **文字过长**：[如「超出一行显示省略号，最多展示 2 行」]
- **空状态**：[如「数据为空时隐藏该模块 / 显示占位文案 '暂无数据'」]
- **最小/最大尺寸**：[如「宽度最小 80px，最大撑满容器」，无约束则填「—」]

---

### B: [子模块名]

**描述**

[一句话说明该模块的视觉职责和业务含义，如「中部内容区，纵向堆叠多个信息行，包含嵌套的图文组合子模块」]

**框架**

```
┌──────────────────────────────────────────────────┐
│  B: [子模块名]  flex-column                      │
│  ┌──────────────────────────────────────────┐    │
│  │  B-1: [名称]  flex-[row/column]          │    │
│  │  ┌──────────────┐  ┌──────────────┐      │    │
│  │  │ B-1-a:[名称] │  │ B-1-b:[名称] │      │    │
│  │  └──────────────┘  └──────────────┘      │    │
│  └──────────────────────────────────────────┘    │
│                  ↓ gap:[N]px                      │
│  ┌──────────────────────────────────────────┐    │
│  │  B-2: [名称]                             │    │
│  └──────────────────────────────────────────┘    │
└──────────────────────────────────────────────────┘
```

**间距**

<!-- ⚠️ gap 在 Max 技术栈中禁止使用，子元素间距统一用 margin-bottom（纵向布局）或 margin-right（横向布局）替代 -->
| 位置 | 属性 | 值 |
|------|------|----|
| 模块内边距 | padding | [上] [右] [下] [左] |
| B-1 → B-2 间距（纵向） | margin-bottom | [N]px |
| 外边距 | margin | [N]px |

**排版**

| 元素 | 字号 | 字重 | 颜色 | 行高 | 对齐 |
|------|------|------|------|------|------|
| [元素名] | [N]px | [400/500/600/700] | [#色值] | [N]px | [left/center/right] |
| [元素名] | [N]px | [400] | [#色值] | [N]px | [left/center/right] |

**CSS**

```css
/* B: [子模块名] */
width: [N]px;                /* 宽度 */
height: [N]px;               /* 高度 */
min-width: [N]px;           /* 最小宽度，无约束则删除此行 */
max-width: [N]px;           /* 最大宽度，无约束则删除此行 */
min-height: [N]px;          /* 最小高度，无约束则删除此行 */
display: flex;               /* 弹性布局 */
flex-direction: column;     /* 主轴方向：纵向 */
flex: [0 0 auto / 1 / N];   /* 弹性伸缩比例，默认 0 0 auto 则删除此行 */
flex-shrink: [0/1];         /* 是否允许收缩，默认 1 */
align-items: [center/flex-start/flex-end/stretch];  /* 交叉轴对齐 */
justify-content: [flex-start/space-between/space-around/space-evenly/center];  /* 主轴对齐 */
padding: [上] [右] [下] [左];  /* 内边距 */
margin: [上] [右] [下] [左];   /* 外边距 */
border-radius: [N]px;        /* 圆角 */
border: [N]px [solid/dashed] [#色值];  /* 无边框则删除此行 */
box-shadow: [N]px [N]px [N]px [N]px [rgba/color];  /* 无阴影则删除此行 */
background: [颜色值];        /* 背景色 */
overflow: [hidden/visible/scroll];  /* 溢出处理 */
position: [relative/absolute/sticky];  /* 默认 static 则删除此行 */
```

**Props**

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `[propName]` | `[string/number/boolean]` | `[默认值]` | [说明] |

**推荐组件**：

| 组件名称 | 类型 | 组件属性与值 | 版本 |
|--------|------|--------|------|
| Max/Leez 组件名 |  Max/Leez | <属性:值> | 组件版本 |

**Mock 数据**

```json
{
  "[字段名]": "[示例文字，如'商品标题']",
  "[字段名]": "[图片地址，如'https://example.com/img.png']",
  "[字段名]": [数值，如 99],
  "[字段名]": true
}
```

**交互状态**

| 状态 | 视觉变化 | 过渡 |
|------|----------|------|
| 默认 | [描述默认样式] | [如「无」，无则填「—」] |
| 按压 / active | [如「背景色加深为 #色值，透明度 0.8」] | [如「200ms ease」] |
| 禁用 / disabled | [如「整体透明度 0.4，不可点击」] | [如「200ms ease」] |
| 加载 / loading | [如「文字替换为菊花图，不可点击」，无则填「—」] | [如「300ms ease-in-out」] |

**边界情况**

- **文字过长**：[如「超出一行显示省略号，最多展示 2 行」]
- **空状态**：[如「数据为空时隐藏该模块 / 显示占位文案 '暂无数据'」]
- **最小/最大尺寸**：[如「宽度最小 80px，最大撑满容器」，无约束则填「—」]

#### B-1: [嵌套子模块名]

**描述**

[说明该嵌套子模块的职责，如「B 内部的图文横排行，左侧图标 + 右侧文字说明」]

**框架**

```
┌──────────────────────────────────────────────────┐
│  B-1: [名称]  flex-row                           │
│  ┌──────────────┐  gap:[N]px  ┌──────────────┐   │
│  │ B-1-a:[名称] │ ──────────▶ │ B-1-b:[名称] │   │
│  └──────────────┘             └──────────────┘   │
└──────────────────────────────────────────────────┘
```

**间距**

<!-- ⚠️ gap 在 Max 技术栈中禁止使用，子元素间距统一用 margin-right（横向布局）或 margin-bottom（纵向布局）替代 -->
| 位置 | 属性 | 值 |
|------|------|----|
| 内边距 | padding | [上] [右] [下] [左] |
| B-1-a → B-1-b 间距（横向） | margin-right | [N]px |

**排版**

| 元素 | 字号 | 字重 | 颜色 | 行高 | 对齐 |
|------|------|------|------|------|------|
| [元素名] | [N]px | [400/500/600/700] | [#色值] | [N]px | [left/center/right] |

**CSS**

```css
/* B-1: [嵌套子模块名] */
width: [N]px;                /* 宽度 */
height: [N]px;               /* 高度 */
min-width: [N]px;           /* 最小宽度，无约束则删除此行 */
display: flex;               /* 弹性布局 */
flex-direction: row;        /* 主轴方向：横向 */
flex: [0 0 auto / 1 / N];   /* 弹性伸缩比例，默认 0 0 auto 则删除此行 */
flex-shrink: [0/1];         /* 是否允许收缩，默认 1 */
align-items: [center/flex-start/flex-end/stretch];  /* 交叉轴对齐 */
justify-content: [flex-start/space-between/center];  /* 主轴对齐 */
padding: [上] [右] [下] [左];  /* 内边距 */
border-radius: [N]px;        /* 圆角 */
border: [N]px [solid/dashed] [#色值];  /* 无边框则删除此行 */
background: [颜色值];        /* 背景色 */
position: [relative/absolute];  /* 默认 static 则删除此行 */
```

**Props**

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `[propName]` | `[string/number/boolean]` | `[默认值]` | [说明] |

**推荐组件**：

| 组件名称 | 类型 | 组件属性与值 | 版本 |
|--------|------|--------|------|
| Max/Leez 组件名 |  Max/Leez | <属性:值> | 组件版本 |

**Mock 数据**

```json
{
  "[字段名]": "[示例文字]",
  "[字段名]": [数值，如 99]
}
```

**交互状态**

| 状态 | 视觉变化 | 过渡 |
|------|----------|------|
| 默认 | [描述默认样式] | [如「无」，无则填「—」] |
| 按压 / active | [如「背景色加深为 #色值，透明度 0.8」] | [如「200ms ease」] |
| 禁用 / disabled | [如「整体透明度 0.4，不可点击」] | [如「200ms ease」] |
| 加载 / loading | [无则填「—」] | [无则填「—」] |

**边界情况**

- **文字过长**：[如「超出一行显示省略号」]
- **空状态**：[如「隐藏 / 占位」]
- **最小/最大尺寸**：[如「—」]

##### B-1-a: [子模块名]

<!--
  每一层嵌套子模块都必须输出完整详细数据，不可只写名称或骨架占位。
  以下为第三层嵌套示例，更深层级结构相同，直到最深层组件。
-->

**描述**

[说明该子模块的职责，如「B-1 内部左侧图标区域，展示优惠券图标」]

**框架**

```
┌──────────────────────────────┐
│  B-1-a: [名称]               │
│  [叶子组件]        │
└──────────────────────────────┘
```

**间距**

| 位置 | 属性 | 值 |
|------|------|----|
| 内边距 | padding | [上] [右] [下] [左] |

**排版**

| 元素 | 字号 | 字重 | 颜色 | 行高 | 对齐 |
|------|------|------|------|------|------|
| [元素名] | [N]px | [400/500/600/700] | [#色值] | [N]px | [left/center/right] |

**CSS**

```css
/* B-1-a: [子模块名] */
width: [N]px;                /* 宽度 */
height: [N]px;               /* 高度 */
display: flex;               /* 弹性布局，叶子组件可省略 */
align-items: [center/flex-start/flex-end/stretch];  /* 交叉轴对齐 */
justify-content: [flex-start/center];  /* 主轴对齐 */
padding: [上] [右] [下] [左];  /* 内边距 */
border-radius: [N]px;        /* 圆角 */
background: [颜色值];        /* 背景色 */
```

**Props**

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `[propName]` | `[string/number/boolean]` | `[默认值]` | [说明] |

**推荐组件**：

| 组件名称 | 类型 | 组件属性与值 | 版本 |
|--------|------|--------|------|
| Max/Leez 组件名 |  Max/Leez | <属性:值> | 组件版本 |

**Mock 数据**

```json
{
  "[字段名]": "[示例文字]"
}
```

**交互状态**

| 状态 | 视觉变化 | 过渡 |
|------|----------|------|
| 默认 | [描述默认样式] | [无则填「—」] |
| 按压 / active | [无则填「—」] | [无则填「—」] |
| 禁用 / disabled | [无则填「—」] | [无则填「—」] |
| 加载 / loading | [无则填「—」] | [无则填「—」] |

**边界情况**

- **文字过长**：[如「—」]
- **空状态**：[如「—」]
- **最小/最大尺寸**：[如「—」]

---

### C: [子模块名]

**描述**

[一句话说明该模块的视觉职责和业务含义，如「底部操作区，固定高度横向排列主次按钮」]

**框架**

```
┌──────────────────────────────────────────────────┐
│  C: [子模块名]  flex-[row/column]                │
│  ┌──────────────────────────────────────────┐    │
│  │  C-1: [名称]                             │    │
│  └──────────────────────────────────────────┘    │
└──────────────────────────────────────────────────┘
```

**间距**

<!-- ⚠️ gap 在 Max 技术栈中禁止使用，子元素间距统一用 margin-bottom（纵向布局）或 margin-right（横向布局）替代 -->
| 位置 | 属性 | 值 |
|------|------|----|
| 模块内边距 | padding | [上] [右] [下] [左] |
| 子元素间距（纵向） | margin-bottom | [N]px |
| 子元素间距（横向） | margin-right | [N]px |
| 外边距 | margin | [N]px |

**排版**

| 元素 | 字号 | 字重 | 颜色 | 行高 | 对齐 |
|------|------|------|------|------|------|
| [元素名] | [N]px | [400/500/600/700] | [#色值] | [N]px | [left/center/right] |
| [元素名] | [N]px | [400] | [#色值] | [N]px | [left/center/right] |

**CSS**

```css
/* C: [子模块名] */
width: [N]px;                /* 宽度 */
height: [N]px;               /* 高度 */
min-width: [N]px;           /* 最小宽度，无约束则删除此行 */
max-width: [N]px;           /* 最大宽度，无约束则删除此行 */
min-height: [N]px;          /* 最小高度，无约束则删除此行 */
display: flex;               /* 弹性布局 */
flex-direction: [row/column];  /* 主轴方向 */
flex: [0 0 auto / 1 / N];   /* 弹性伸缩比例，默认 0 0 auto 则删除此行 */
flex-shrink: [0/1];         /* 是否允许收缩，默认 1 */
align-items: [center/flex-start/flex-end/stretch];  /* 交叉轴对齐 */
justify-content: [flex-start/space-between/space-around/space-evenly/center];  /* 主轴对齐 */
padding: [上] [右] [下] [左];  /* 内边距 */
margin: [上] [右] [下] [左];   /* 外边距 */
border-radius: [N]px;        /* 圆角 */
border: [N]px [solid/dashed] [#色值];  /* 无边框则删除此行 */
box-shadow: [N]px [N]px [N]px [N]px [rgba/color];  /* 无阴影则删除此行 */
background: [颜色值];        /* 背景色 */
overflow: [hidden/visible/scroll];  /* 溢出处理 */
position: [relative/absolute/sticky];  /* 默认 static 则删除此行 */
```

**Props**

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `[propName]` | `[string/number/boolean]` | `[默认值]` | [说明] |

**推荐组件**：

| 组件名称 | 类型 | 组件属性与值 | 版本 |
|--------|------|--------|------|
| Max/Leez 组件名 |  Max/Leez | <属性:值> | 组件版本 |

**Mock 数据**

```json
{
  "[字段名]": "[示例文字，如'确认']",
  "[字段名]": false,
  "[字段名]": [数值，如 1]
}
```

**交互状态**

| 状态 | 视觉变化 | 过渡 |
|------|----------|------|
| 默认 | [描述默认样式] | [如「无」，无则填「—」] |
| 按压 / active | [如「背景色加深为 #色值，透明度 0.8」] | [如「200ms ease」] |
| 禁用 / disabled | [如「整体透明度 0.4，不可点击」] | [如「200ms ease」] |
| 加载 / loading | [如「文字替换为菊花图，不可点击」，无则填「—」] | [如「300ms ease-in-out」] |

**边界情况**

- **文字过长**：[如「超出一行显示省略号，最多展示 2 行」]
- **空状态**：[如「数据为空时隐藏该模块 / 显示占位文案 '暂无数据'」]
- **最小/最大尺寸**：[如「宽度最小 80px，最大撑满容器」，无约束则填「—」]

---

### [继续按 D / E / F... 扩展，若有嵌套则在该节点下继续展开子层级，结构同上，直到最深层组件]

---

## 4 组件统计

- **总模块数**：[N]
- **Max 组件**：[N] 个
- **Leez 组件**：[N] 个

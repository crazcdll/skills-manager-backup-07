# ADR-0001 知识库结构决策

> 文档更新时间：{{TODAY}}
> Owner：{{OWNER}}
> 适用范围：`{{REPO_NAME}}` 知识库结构设计
> 关联代码：`{{REPO_PATH}}`
> 关联文档：`knowledge/README.md`

## 背景

说明为什么要在仓库内建设知识库。

## 问题

说明希望解决什么问题，例如新人上手、需求评估、排障效率。

## 备选方案

- 只有 README
- 单目录堆积式文档
- `Agent + business + modules` 分层结构

## 决策

采用 `Agent + business + modules + onboarding + faq + adr` 的结构。

## 影响

说明对日常维护和协作的影响。

## 后续观察点

记录需要持续验证的点。

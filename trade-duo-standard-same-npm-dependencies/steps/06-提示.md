---
name: 提示
description: 提示用户，需要重点关注的内容
---

## 🎯 执行内容

**忽略临时目录**：检查工程根目录（或当前仓库根目录）的 **`.gitignore`**，若尚未包含本流程产生的临时目录，则追加一行 **`.temp/`**（或 **`.temp`**，与团队约定一致），避免将 `.temp/trade-duo-standard-same-npm-dependencies/` 等中间产物提交入库；若已存在则无需重复添加。

提示用户重点关注@mtfe/msi-mrn和@mtfe/msi 版本的升级，对于跨端项目，h5上可能出错。

提示用户重点检查 `dependencies.json` 中的版本改动是否符合预期。

若工程存在并已修改 `componentsMap.json`，提示用户核对物料映射中的 `npmVersion` 与资源 URL 是否与清单一致。

若存在 ${{outputFile:.temp/trade-duo-standard-same-npm-dependencies/result/block-commit.flag}}（步骤 4 因降级风险生成），**须明确提示用户：未完成人工核对前不要提交**，并说明需处理 changelist 中 **【步骤 4 降级风险 · 禁止提交】** 小节所列项；确认后再删除该标记文件并提交。

根据${{inputFile:.temp/trade-duo-standard-same-npm-dependencies/result/changelist.md}}的改动内容总结给用户总结，有要注意的信息也一并告诉用户。

---
*完成此步骤后，请继续执行下一步*

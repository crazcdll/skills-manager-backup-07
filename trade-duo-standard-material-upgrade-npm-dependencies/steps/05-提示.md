---
name: 提示
description: .gitignore、注意事项、最终汇报表格模板
---

## 执行

- **结项须含第 4 步**（清单依赖检查：`✅` / `🚨`）。
- **`.gitignore`**：无则加 **`.temp/`**，勿提交 `.temp/trade-duo-standard-material-upgrade-npm-dependencies/`。

## 注意事项

1. **学城优先**：版本以 Citadel 拉 **2738858921** 为准；失败才用 `data/fallback-target-versions.md`，并写明降级。
2. **预发精确**：含 `-test.`、`-beta.` 等版本不要乱加 `^`、`~`。
3. **`@mtfe/msi` 与 `@mtfe/msi-mrn` 同版**：清单里两者要一起改。
4. **固定三件套**：`react`、`react-redux`、`redux` 按 `data/fallback-target-versions.md` 固定值。
5. **本 skill 边界**：不 `yarn install`、不改 `yarn.lock`、不维护 `resolutions`。
6. **第 3 步“自助”包**：结项时再列一遍包名。
7. **`componentsMap`**：本 skill 不改；若需同步，按团队规范另处理。

## 最终汇报（必须用表格）

完成后，**不要用列表**，直接输出如下 Markdown 表格；无内容写 `无`、`不适用` 或简短说明。

| 项目 | 内容 |
|---|---|
| `dependencies.json` 变更 | 列出变更包名与 `旧版 -> 新版`；无则写 `无` |
| `ohDependencies.json` 结果 | 迁出结果、待自助包、是否已空 |
| 锁文件相关 | 本 skill 不涉及 `resolutions`、`yarn.lock` |
| 第 1 步结论 | `Citadel 成功` 或 `已降级到 data/fallback-target-versions.md` |
| 第 4 步结论 | 清单依赖检查结果：`✅` 或 `🚨` + 位置 |
| 提交建议 | 建议提交两份 JSON；勿提交 `.temp/` |
| 其他 | 可选，如 `componentsMap` 是否需人工同步 |

若需要补充“待用户自助”的具体包，可在主表后再追加一张表：

| 待用户自助包 | 当前版本 | 说明 |
|---|---|---|
| `<name>` | `<version>` | 未出现在目标清单中，需自助确定三端版本 |

基于 `changelist`、第 3 步汇总、第 4 步结论和 `git diff` 输出，勿编造。

*结束。*

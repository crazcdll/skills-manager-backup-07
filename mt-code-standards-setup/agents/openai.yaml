interface:
  display_name: "统一编码规范接入工具"
  short_description: "按仓库名称或 Git 地址同步有效 L1 与 L2 规则包"
  default_prompt: "Use $mt-code-standards-setup to identify the repository I provide, or discover the current Git repository, and safely synchronize its platform-authorized L1 and L2 rule bundle into my chosen output directory."
policy:
  allow_implicit_invocation: true
